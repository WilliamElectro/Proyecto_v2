@page "/workers/edit/{Id:int}"

<h3>Editar Trabajador</h3>

@if (worker is null)
{
    <Loading />
}
else
{
    <WorkerForm @ref="workerForm" Worker="worker" OnValidSubmit="EditAsync" ReturnAction="Return" />
}
