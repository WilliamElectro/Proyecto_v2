@page "/workers/edit/{Id:int}"

<h3>Editar Trabajador</h3>

@if (worker is null)
{
    <Loading />
}
else
{
    <WorkerForm @ref="workerForm" Worker="worker" AvailableModalities="AvailableModalities" OnValidSubmit="EditAsync" ReturnAction="Return" />
}
